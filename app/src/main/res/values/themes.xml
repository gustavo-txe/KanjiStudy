<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main App Theme -->
    <style name="Theme.KanjiStudy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional custom colors -->
    </style>

    <!-- Splash Theme -->
    <style name="Theme.KanjiStudy.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/iconkanji</item>

        <!-- VERY IMPORTANT -->
        <item name="postSplashScreenTheme">@style/Theme.KanjiStudy</item>
    </style>

</resources>
